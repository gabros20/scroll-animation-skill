{
  "name": "scroll-animation-skill-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Runnable validation for the scroll-animation skill's assets. Not the skill itself -- see scroll-animation/.",
  "type": "module",
  "scripts": {
    "pretest": "mkdir -p tests/.scratch tests/smoke-gsap/public && ffmpeg -y -f lavfi -i \"testsrc=size=640x360:rate=30:duration=2\" -g 1 -pix_fmt yuv420p tests/.scratch/clip.mp4 && cp tests/.scratch/clip.mp4 tests/smoke-gsap/public/clip.mp4",
    "test": "npm run test:types && npm run test:smoke && npm run test:audit",
    "test:types": "tsc --noEmit -p tests/tsconfig.react-motion.json && tsc --noEmit -p tests/tsconfig.gsap.json",
    "test:smoke": "node tests/run-smoke.mjs",
    "test:audit": "node scroll-animation/scripts/audit-motion.mjs --selftest"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "gsap": "^3.13.0",
    "motion": "^12.0.0",
    "playwright": "^1.48.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
