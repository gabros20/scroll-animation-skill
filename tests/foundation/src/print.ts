// The engine boots at once, so the failsafe never runs and nothing reveals the entrance items
// on screen: only the print rules can.
import { markAnimationReady } from '../../../skills/scroll-animation/assets/config'

markAnimationReady()
